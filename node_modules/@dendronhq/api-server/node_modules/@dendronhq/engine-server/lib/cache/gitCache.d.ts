import { DNodeType, NoteProps } from "@dendronhq/common-all";
import { Git } from "../topics/git";
declare type DEngineCache = any;
declare type GitCacheOpts = {
    root: string;
};
export declare class GitCache implements DEngineCache {
    opts: GitCacheOpts;
    git: Git;
    entries: {
        [key: string]: NoteProps;
    };
    constructor(opts: GitCacheOpts);
    get(key: string): Promise<NoteProps | null>;
    getAll(type: DNodeType, checkpoint: any): Promise<NoteProps[]>;
    setAll(type: DNodeType, entries: NoteProps[], checkpoint: any): Promise<void>;
}
export {};
