import { NoteProps } from "@dendronhq/common-all";
import execa from "execa";
import _ from "lodash";
export declare type DHookFunction = (opts: {
    note: NoteProps;
    execa: typeof execa;
    _: typeof _;
}) => Promise<NoteProps>;
